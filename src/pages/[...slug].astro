---
export const prerender = true

export async function getStaticPaths() {
  const pages = [
    {
      slug: undefined,
    },
  ];
  return pages.map(({ slug }) => {
    return {
      params: { slug },
    };
  });
}
---
